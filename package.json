{
  "name": "boom-crash-predictor",
  "version": "1.0.0",
  "private": true,
  "description": "SpikeScope — live Boom 1000 / Crash 1000 opportunity feedback from Deriv charts",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n server,client -c teal,magenta \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "start": "npm run start --prefix server",
    "test": "npm run test --prefix server"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Delanoso/Deriv-tader.git"
  },
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
